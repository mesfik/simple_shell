#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int age;

	printf("Enter your age : ");
	scanf("%d", &age);
	printf("your age is %d\n", age);
	return (0);
}
